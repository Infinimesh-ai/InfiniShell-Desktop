# 完整 Codex 包的同提交 macOS 适配器复验

干净提交 `37b0bc73288aab5be4d0d151796eea557be4c0fb` 已用完整官方 Codex 0.147.0 包通过三项真实模型验证。测试二进制与监督进程均由该提交构建，命令、二进制摘要和逐事件记录见 [完整报告](validation/macos-codex-complete-runtime-37b0bc732.json)。本次没有启动或操作 GUI。

| 用例 | 实际结果 |
| --- | --- |
| 生命周期 | 新建、两轮完成、固定命令单次允许和拒绝、运行中追加生效、取消得到原生 Cancelled、关闭进程并按同一原生会话继续；恢复输出含追加指令的唯一标记 |
| 本地工具与恢复 | 注册的本地工具真实调用、关闭后恢复同一会话、再次调用工具；原生保存的工具定义继续可用 |
| 图片输入 | 真实 localImage 输入及预期内容断言通过 |

此次使用的完整包包含主程序、同版本 `codex-code-mode-host`、rg、zsh 及官方布局元数据。主程序与工具宿主的摘要在执行前重新核对。实际审批允许后的工具执行通过，说明原先私有单文件副本缺宿主的问题已在此适配器场景消除；先前 GUI 文件读取和审批失败仍保留，不改写为成功。

监督进程构建耗时 235.460 秒，退出 0。三项测试均由独立进程运行，每项创建自己的临时 CODEX_HOME，使用已有授权的凭据副本；记录不包含凭据。每项同时要求 libtest 恰好通过一项及对应原生验收终态，不能凭命令退出 0 或零项测试判为成功。

这些结果只证明 macOS Rust 适配器与进程恢复。GUI 应用重启、父子任务 UI、双语布局、Windows/Linux 模型执行、Claude/Grok 生命周期和最终全平台同提交验收均未由本报告覆盖。后续 `c46714435` 仅修改探针与文档，仍应使用其自己的原生平台证据，不将此报告改标为新提交。
