# Grok skills reload 与 MCP 目录通知契约定向核对

本次核对把 source27 的未知 25 字节通知精确识别为 `_x.ai/mcp/servers_updated`；`skills-reload` 的精确调用方仍未知。它们属于不同协议事务，不能把通知或内部响应转换成提示回合完成、审批验证或 SDK 来源成功。source27 两项真实失败保持原结果；本次仅为静态契约审查，没有执行原生 CLI、模型请求或 Cargo。

已实际读取官方公开候选快照 `482711333c7195dc16a272777f86086d615e2afb` 的递归树一次（HTTP 200，`truncated=false`），以及 12 个定向 raw 源码文件（全部 HTTP 200，合计 578998 字节，每个小于 256 KiB）。复用的官方公开缓存也来自同一快照。逐文件 URL、真实字节、SHA-256、时间和必要代码行保存在 [安全 JSON](validation/grok-skills-reload-contract-review.json)。该快照与已发布 Grok Build 1.0.30 固定二进制的完整构建来源映射仍未证明；候选 `SOURCE_REV` 与原生 build ID 的边界沿用此前记录。

所有字符串摘要均按原 UTF-8 字节计算，**不包含 JSON 引号**。真实 source27 公共诊断的 25B/SHA `5ad0b9eadd8fadb2225bf5c00b21c1cf42ab869332b86333e722aa248a106580` 精确等于 `_x.ai/mcp/servers_updated`。候选 [extensions/mcp.rs:40](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-shell/src/extensions/mcp.rs#L40) 定义不带下划线的 `x.ai/mcp/servers_updated`；[leader/protocol.rs:541–543](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-shell/src/leader/protocol.rs#L541-L543) 明确 ACP 扩展线协议使用 `_` 前缀。此处比较的是明确的固定构造，并非猜测未知字符串。该常量在定向源码中有两处匹配声明/引用：[leader/server.rs:465–479](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-shell/src/leader/server.rs#L465-L479) 将它归为没有 `sessionId`、应广播到所有客户端的 MCP 目录变化。

候选 [extensions/mcp.rs:225–234](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-shell/src/extensions/mcp.rs#L225-L234) 定义 `McpServersUpdated { mcp_servers: Vec<McpServerEntry> }`，经 camelCase 序列化为 `{ "mcpServers": [...] }`，不含 `sessionId`。其 [287–301 行](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-shell/src/extensions/mcp.rs#L287-L301) 明确在启动目录 MCP 发现之后，以 `ExtNotification::new(SERVERS_UPDATED, params)` 推送目录，避免阻塞 initialize。目录项 [70–168 行](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-shell/src/extensions/mcp.rs#L70-L168) 包含 name/source、HTTP/stdio/managedGateway 类型与可选 session 工具状态，stdio env 具有 name/value；只能输出经过白名单投影的名称摘要、类型、数量，不能直接公开整个目录。因此 source27 的无 session ID 形态与候选协议相符，但本次没有读取真实参数正文，不能宣称其完整 schema 或目录闭包已经通过。

13B/SHA `731058153ec89c5e512596d24d5ac95c7262a9cb4e5c58fbdcebd36677f9b38e` 沿用前次固定二进制的单一 `skills-reload` 字面量匹配证据（offset 116151162，count 1）。本次 12 个候选源码文件没有该字面量，不能证明其实际注入路径。已找到候选 [leader/protocol.rs:530](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-shell/src/leader/protocol.rs#L530) 的内部方法 `x.ai/internal/reload_skills`，以及 [558–567 行](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-shell/src/leader/protocol.rs#L558-L567) 的 `internal_request_line(id: &str, method, params)`：它接受字符串 ID、构造 `_x.ai/internal/reload_skills` 请求。候选 [extensions/session_admin.rs:607–615](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-shell/src/extensions/session_admin.rs#L607-L615) 对 ReloadSkills 调用 `reload_skills_all_sessions()`，以 `ExtMethodResult::success({reloaded})` 返回。另一个直接内嵌 pager 路径 [acp/spawn.rs:449–500](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-pager/src/acp/spawn.rs#L449-L500) 收到 SkillsFileWatcher 的 DiscoveryChange::Skills 后直接调用 agent 方法，没有在该段构造外部 RPC ID。不能把此直接路径冒充 native stdio 已观察响应的调用方。

根代理新增的 [公共字段形状](validation/grok-27-native-response-field-shapes.json) 与原 259B envelope 对账，只有外层 result 对象中的一个 6B 键，其 SHA `f6a214f7a5fcda0c2cee9660b7fc29f5649e3c68aad48e20e950137c98913a68` 精确等于固定协议键 `result`，值类型为 object。候选 [session/result.rs:30–37,85–91](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-shell/src/session/result.rs#L30-L37) 确实将 `ExtMethodResult {result,error?}` 再序列化进 ACP ExtResponse；所以双层 result carrier 有源码依据，但未读取 inner 值，不能声称它就是 `{reloaded: ...}`。本次没有读取任何私有 envelope、原生/模型/错误正文或认证文件。

最小适配建议如下，尚未实施：

- 在 policy preflight 的 `notification()`（当前 grok_policy_preflight_live_tests.rs:502–552）中，先于 session 绑定单独处理精确 `_x.ai/mcp/servers_updated`，验证无 id/result/error、对象参数和有界目录数组；保留仅预注册 server 的闭包校验及零 prompt 守卫。全局目录通知不调用 `bind(session)`，不创建原生输入身份，不提升权限或 ceiling。未知方法和额外 server 继续拒绝。该变化可修正已识别的协议形态缺口，仍须真实复验实际参数 schema。
- 当前生产 grok.rs:1621–1646 对响应使用数值 ID，并要求匹配实际 pending；这保护着 prompt 4 的归属。不能仅因已识别字符串 `skills-reload`，就忽略它、消费 prompt pending 或视为原生 ACK。若应用主动发起 skill list/reload 等维护请求，应使用真实 outbound 请求自己的 ID/generation/请求种类账本，独立验证扩展结果 envelope，和 MCP SDK outer/inner ID 分开。仅发送一个新的 owned request，不能令之前未拥有的字符串响应自动获得归属，也没有证据证明会阻止该内部响应。
- 若固定原生 stdio 的内置监视器确实注入维护事务并把响应转发到同一 stdout，仍需要核实该固定接口的注入调用方或明确隔离/路由契约，再设计封闭维护事务处理。此次有限源码检索未得到这一证据。目录 ready、双层 result 和工具列表均不证明 inspect 发生、SDK 来源、原生任务完成或根权限上限。

能力或模型配置与该字符串响应的因果关联没有在所读文件中得到证明。所有门禁、真实接口验收与最终产品能力保持原有状态；没有因为本报告将 unknown 改成 verified。无需本地化变更：本次仅新增验收文档与协议证据，未改变任何用户可见产品行为。

STOP；finished_reads_done=true。
