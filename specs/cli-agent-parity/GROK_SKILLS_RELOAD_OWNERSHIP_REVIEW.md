# Grok `skills-reload` 请求所有权定向审查

已找到固定公开候选源码中的明确调用方：CLI 内部的技能文件监视器使用固定字符串 ID `skills-reload`，向与用户 stdin 共享的 ACP 输入写入 `_x.ai/internal/reload_skills` 请求，参数为 `{}`。App 生产 transport、MCP 桥接器、准备器和插件没有主动发出这个固定 ID。因此不能在 App “发出时”补一个不存在的维护 RPC pending，也不能用响应摘要追认它属于正在等待的 prompt 4。

本次只读主树相关源码、显式官方公开缓存与同一候选快照 `482711333c7195dc16a272777f86086d615e2afb`；实际 GitHub tree 请求一次，raw 请求 8 次、7 个不同文件（模块定位时重读 mvp_agent/mod.rs 一次），均 HTTP 200，累计 raw 886370 字节，每文件小于 256 KiB。原始宽泛检索上下文已从交付证据收敛为必要源码行。全部 URL、时间、bytes、SHA-256 与边界见 [安全 JSON](validation/grok-skills-reload-ownership-review.json)。没有读取认证、私有配置、用户 DB、private envelope、原生 stdout、模型/错误正文或二进制 strings；没有 CLI、模型、Cargo、GUI 或 Git mutation。

App 的 grok.rs:659–666 为请求分配递增数值 ID 并记录 PendingRequest；467–474 没有技能重载维护请求种类，398–459 将 Effects 写入真实 stdin。grok_local_tools.rs:85–100 验证已注册 SDK 反向调用，466–467 只回显其 outer ID 构造响应；字符串回显的存在不等于主动发起 `skills-reload`。SDK 专用准备器 run_grok_sdk_origin_probe.py:147–168 只替换启动参数和沙箱权限；共享包装器 run_grok_adapter_live.py:314–333 以 execv 原样运行原生程序，没有在 stdin 注入 RPC。Grok 插件 notify.cjs:185–209 接收 hook 输入后向 /dev/tty 或 CONOUT$ 写状态序列，不向 hook stdout 返回 RPC/审批决定。在这些检索域中，`skills-reload` 的仅有字面量匹配是两处拒绝测试。

候选入口 [main.rs:1559–1562](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-pager-bin/src/main.rs#L1559-L1562) 调用 agent_command；[agent_command.rs:69–84](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-pager-bin/src/agent_command.rs#L69-L84) 再调用 runtime.run_stdio。候选 [agent/app.rs:152–203](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-shell/src/agent/app.rs#L152-L203) 的 spawn_skills_file_watcher 在 DiscoveryChange::Skills 的两个分支均固定 `(skills-reload, ReloadSkills)`（183、187 行），196 行以 `{}` 生成内部请求，198 行将其写入 skills_tx。已有 [leader/protocol.rs:530,541–543,558–567](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-shell/src/leader/protocol.rs#L530-L567) 将 ReloadSkills 映射为 `x.ai/internal/reload_skills`，线协议明确加 `_` 前缀，并接受字符串 ID。

候选 [agent/app.rs:240–258](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-shell/src/agent/app.rs#L240-L258) 将 outgoing 绑定到 stdout，创建 simplex ACP 输入；真实 stdin reader 和技能 watcher 各持同一 acp_incoming_tx 的 clone。其 [119–150 行](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-shell/src/agent/app.rs#L119-L150) 将 incoming/outgoing 传给 AgentSideConnection::new，返回该连接的 handle_io；GatewayReceiver 接收 gateway 通道，使用同一连接发送 agent 到客户端的调用/通知。watcher 只持写入端，没有 response reader、pending 或接收确认。这解释了内部请求为什么会经 ACP response writer 写到 App 读取的同一 stdout；所读 stdio 管线没有按内部 ID 消费/过滤响应的代码。此结论限于候选源码的明确流绑定：本次没有读取第三方 ACP writer 和完整 GatewayReceiver 实现，也未独立证明 runtime 前端委托的完整实现或固定二进制执行栈。

watch 范围与触发条件有具体源码：[config/watcher.rs:571–599](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-shell/src/config/watcher.rs#L571-L599) 使用 cwd、可选 workspace-user 目录、GROK_HOME 与 skills.paths，按 CompatConfig::default 收集 vendor 目录超集，并额外监视 bundled；此层不是按单个 session profile 关闭 watcher。其 [295–325 行](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-shell/src/config/watcher.rs#L295-L325) 将 vendor 根名 `.grok/.agents/.claude/.cursor`、`skills/commands/SKILL.md/bundled`、skills 后代和 commands 下的 .md 归为 Skills；workflows 为另一类。287 行设置 2 秒 debounce，[25–27 行](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-shell/src/config/watcher.rs#L25-L27) 过滤读访问事件；605–669 行收到变更后发出 DiscoveryChange。具体发现根由 [prompt/skills.rs:137–216](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-agent/src/prompt/skills.rs#L137-L216) 从现存 project_sources.skill_dirs() 的 vendor 目录、workspace-user、GROK_HOME、用户 vendor 目录和显式 paths 的目录/文件父目录收集。它证明触发类别，不能证明 source27 到底哪个路径变更或谁修改了该路径。

候选返回语义也已核实：[extensions/session_admin.rs:607–615](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-shell/src/extensions/session_admin.rs#L607-L615) 用 ExtMethodResult::success({reloaded}) 返回；[agent_ops.rs:72–79](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-shell/src/agent/mvp_agent/agent_ops.rs#L72-L79) 的 reload_skills_all_sessions() 明确返回 usize，值为 resident_ids().len()。它遍历 resident session、发送 SessionCommand::ReloadSkills，并忽略 send 错误。因此 reloaded 是 resident 快照计数/异步派发数量，不是技能数量，也不证明所有重载真正完成。[session/result.rs:30–37,57–73,85–91](https://github.com/xai-org/grok-build/blob/482711333c7195dc16a272777f86086d615e2afb/crates/codegen/xai-grok-shell/src/session/result.rs#L30-L91) 定义 `{result,error?}`，success 的 error 省略，失败的 result 可为 null、error 为错误值；序列化错误转成外层 ACP internal_error。对应候选闭合成功形态为：

```json
{"jsonrpc":"2.0","id":"skills-reload","result":{"result":{"reloaded":0}}}
```

上例仅示意字段结构，0 不是已观察原生值；reloaded 实际是非负整数。任何 error、额外字段或不同类型都不能伪装成这个成功形态。success 只说明维护 handler 返回，不是原生用户回合或子任务完成。

已有 source27 [公共 envelope 形状](validation/grok-27-native-response-field-shapes.json) 仅证明 outer result 中有一个键：6B/SHA `f6a214f7a5fcda0c2cee9660b7fc29f5649e3c68aad48e20e950137c98913a68`，匹配 result，值为 object。13B/SHA `731058153ec89c5e512596d24d5ac95c7262a9cb4e5c58fbdcebd36677f9b38e` 对应 fixed literal skills-reload；本次候选也有两个明确调用点。但这没有证明 native inner 字段是 reloaded、类型正确、没有 error/extra，也没有证明已发布固定 binary 的所有源码与候选一致。不能据此直接放宽生产响应 ID 守卫。

最小兼容方案应先补一个新的、有界、testcfg 安全 inner-shape 诊断，核对固定实际 binary 的 exact string ID 与封闭 success 字段/类型；只导出固定字段匹配、类型、数量与布尔结论，不读回或公开原生值/正文。契约真实确认之后，可单独识别当前独占进程传输域中的这一固定内部维护成功响应：严格 JSON-RPC 2.0、三字段 frame、无 method/error/extra、双层 result 均闭合、reloaded 为有界 unsigned integer。它返回零业务 Effects，不消费或替代数值 userRPC pending，不给 SDK origin、native ACK、权限/ceiling、用户任务终态或结果回收任何证明。

这种观察账本必须与 App owned 请求账本分开，并按实际 transport generation 隔离。watcher 合法地重用固定 ID，resident 数量也可以变化，不能套用用户 RPC 的“相同 ID 永远只能同 fingerprint”规则；重复、乱序维护响应没有业务副作用，继续受帧/字节预算约束。未知 ID、畸形/error 或未确认 native shape 保持拒绝。仅让 App 新发一个 reload 请求不会阻止 CLI 内部注入，也不会让旧响应获得 App 所有权。若改上游，更直接的修复是内部注入使用 notification 或绑定内部 reply sink，避免维护 response 落入客户端 stdout；本次没有修改上游或本仓库源码。

所有旧 FAILED 结果、生产门禁、Grok SDK/审批/业务来源状态保持原有规则。此次没有原生复验或任何 interface PASS；下一次安全形状证据仍是接受固定 native 契约的必要条件。无需本地化变更：本次只新增审查文档及公开安全证据。

STOP；finished_reads_done=true。
