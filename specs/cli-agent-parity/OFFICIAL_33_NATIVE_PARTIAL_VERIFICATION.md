# source33 原生部分验收

本批使用父提交0059上的同一126项输入候选及实际新构建 worker，尚未提交，不能计作最终同提交验收。[逐文件记录](validation/macos-official-33-native-artifacts.json)。

- main 构建退出0，273.701秒；严格签名通过，完整英文与简体中文资源已嵌入，测试库前后保持一致。
- Grok插件六阶段验收退出0：真实安装、已知0.1.0完整配方升级至0.1.1、同版本更新、禁用后拒绝更新、文件事务注入失败回滚、回滚后生产更新。文件事务故障步骤不伪称完整原生安装失败。安装与升级后，实际缓存中的模块导出版本均为0.1.1。另以受控输入调用安装缓存脚本main，经macOS控制终端收到正确通知；该输入由验证器提供，不是Grok原生事件触发，也不计Windows CONOUT$通过。
- policy5整体失败：原生初始化不再提供cached_token，未发起认证或模型输入。失败清理取得真实nullable退出码回执，stdout关闭与资源回收确认通过。随后官方login --oauth已利用现有浏览器会话成功刷新隔离授权。
- policy6使用明确记录SHA的独立非空只读profile（并非冻结清单内旧profile）。认证成功，session/new已发出；预检拒绝`_x.ai/session_notification`后，退出排空期间收到该RPC响应。19项公共事件中只有3个已发送RPC；没有模型输入。响应未由正常请求路径完成验证，因此不计New成功或完整权限调查通过。

旧policy4的422字节错误响应可由公开源码错误常量精确重建，SHA与归档一致，原因是关闭默认工具后配置了空工具集。修正profile只增加GrokBuild:read_file，尚不证明子任务权限上限、文件系统沙箱或SDK工具授权。

原始失败保留。下一步将补齐预检对生产已支持元数据通知的处理，并完成生产SDK租约接入、真实完成回执期限及在线验证。父子任务、最终完整三方流程、同提交跨平台、GUI双语与SSH/tmux仍未完成。
