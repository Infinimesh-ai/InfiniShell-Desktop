# source35 本地与界面验证

本次基于 `af1040dc8e7522ccafb09d8f0d4c6b219bd9d607` 的隔离候选，145 项源码/夹具输入中修改 27 个文件。不是最终提交验收，Goal 继续进行。

## 功能变更

- Grok 可显式开启任务查看与消息；子任务派发仍受父权限检查约束，尚未开放。
- 本地派发请求使用独立的三 CLI 类型，保留 Grok 身份；共享云端协议不冒充支持 Grok。
- 技能人工菜单按 CLI 实际规则处理 `user-invocable`：Grok 接受布尔 true、精确字符串 true 或缺省；Claude 的布尔 false 隐藏菜单；Codex 保留原规则。统一覆盖 GUI/TUI 技能选择、搜索及输入识别，未开放 Grok 托管图片/技能。
- 权限预检兼容官方 `model_changed` 推理强度及模型目录通知，仍不将元数据当权限证明。

## 本地结果

| 门禁 | 退出码 | 通过数量/状态 | 用时 |
| --- | --- | --- | --- |
| check | 0 | 通过 | 100.83 秒 |
| i18n | 0 | 11 | 256.569 秒 |
| focused | 0 | 1639 | 23.258 秒 |
| ai_skills | 0 | 66 | 37.529 秒 |
| Python 权限预检模块 | 0 | 60 | 0.082 秒 |

13 个新增或重命名的 warp 回归及 2 个共享 ai 回归均各执行一次并通过。其余 Python/Node 模块未改动，本次不重复执行或累计冒充新增验证。`git diff --check -- app crates script` 通过。

首次编译真实失败于 `serde_yaml 0.8` 不存在的 Tagged 变体及 Mapping::get 参数类型，修正为本仓库接口后重跑通过。原失败日志和输入摘要保留，不改写首次结果。新技能测试有一处 `unused_mut` 警告，待下一批清理。

本地化审计：任务消息/子任务提示已同步英文与简体中文，其余解析和筛选未引入可见文本；i18n 11 项通过。英文和简体中文的三 CLI 管理入口、Grok 消息开关及子任务限制提示已实际检查。main 构建 275.766 秒退出 0，严格签名通过，完整双语资源均嵌入。TUI 相关测试分两次执行：工具标签 9 项、实际 slash/input 模块 143 项通过；独立 TUI 在 tmux 内打开 `/skills` 成功，没有发送模型输入，不代表三款 CLI 的 TUI 全链。

## 普通 Grok 终端实测

[32 张截图与安全摘要](validation/gui-official-35/verification.safe.json)记录固定 Grok 1.0.30 的正式 11 次输入，另有一次独立会话中误输入 `grok` 的历史，不混入正式链。三条额外原生 user 记录是启动上下文，不能直接把 history 行数当人工输入次数。

- 中文多行富输入、英文第二轮追问均返回原问候；中文含空格路径由文件选择器传入，原生读取后返回唯一标记。
- 原生技能菜单隐藏 `user-invocable: false`，可见技能实际读取正文并返回标记。应用自身的非 Git 项目技能菜单仍为空，此项失败由 source36 修复后另验。
- Write 审批选择一次允许，文件为预期标记加一个 LF；一次拒绝后文件不存在。待审批主动取消后文件也不存在。
- 运行中追加输入进入原生队列，随后取得追加指令要求的结果。
- 正常结束 CLI 后显式 `--resume` 同一个原生 ID，正确回忆早期问候。应用正常退出重开，历史摘要不变且没有 Grok 进程；再次显式恢复后发送唯一新输入，返回原文件标记 `GROK_FILE_CONTEXT_35_6dbe`。

上述是普通 PTY 的可见结果，不将 Stop hook 或屏幕文本升级为托管任务的成功状态。验收应用及子进程已退出，独立认证副本已删除，原始认证未修改。曾发生 CUA 在退出后自动重开而缺少隔离环境，该窗口已排除；正式重启使用完整独立环境。

## 通知插件发现

生产安装器安装 0.1.1 后，`/hooks` 按 `r` 才实际显示九个 hooks。仅重启前台 CLI 的提示不充分，source36 已同步修正中英文成功提示。

插件通知仍失败。仅在隔离缓存中临时加入不含提示词或认证的诊断，正常退出时 SessionEnd、Stop 均进入生产 main 且身份归一化成功，但打开 `/dev/tty` 返回 `ENXIO`；hook 子进程没有控制终端。诊断后已恢复原生产字节。这是实际运行差异，原先附着控制终端的单独脚本测试不能覆盖。修复及脱离控制终端的回归在后续候选中实施，不能将本轮记为通知通过。

## 当前边界

Grok 租约真实成功属于 source34b lease1，本次不声称父子派发完成。policy8 无模型输入探针失败：四个可选诊断接口中的首个返回 method-not-found，后续候选补齐有限降级；不证明权限上限。source36 固定创建策略已接入并通过本地回归，真实固定策略与父子链尚待验证。[CI14 终态报告](CI_14_FINAL_VERIFICATION.md)中 Linux 所选门禁通过、Windows 仅 Codex 缓存恢复探针失败；测试的是 af1040，不包含本批改动。最终全工作区、同提交三平台和 SSH/tmux 整链仍待完成。
