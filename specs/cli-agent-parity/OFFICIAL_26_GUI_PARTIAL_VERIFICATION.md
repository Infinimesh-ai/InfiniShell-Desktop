# source26 实际 GUI 部分验收

本轮使用实际提交 `436cc739234061c092cedd106432bbbfa3c2d645` 的 macOS 应用构建副本，通过 Computer Use 的 `@oai/sky` 接口操作真实窗口。原构建二进制 SHA-256 为 `d8102b9d02cd9f104d0d2c2098ca805c99abf6ac60441bd51b84356d3fd12de5`；为隔离应用身份重新签名后的副本 SHA-256 为 `2c0d7c02c7dd6cf21ee6088b0aa04dd8b4ea66287d6fb4be4dc3f4132a0a05ad`，应用 ID 为 `dev.infinishell.InfiniShellParityOfficial26`。副本、HOME、项目、应用数据域和 CLI 配置目录均单独创建，不操作用户日常应用实例。

截图和有限 SQLite 证明位于 [gui-436cc739](validation/gui-436cc739/)。本报告只记录已实际观察的范围，不计三款 CLI 的完整 GUI 链或 Goal 通过。

## 安装检测、标题栏及双语布局

无认证材料的阶段，标题栏同时显示 Claude、Codex、Grok 图标。第三方 CLI 设置页显示三者已安装，工具栏、标签页菜单和标题栏的默认可见性均启用。实际本地任务窗口识别版本为 Codex CLI `0.147.0`、Claude Code `2.1.273`、Grok Build `1.0.30`。因此登录不是显示标题栏图标的前提；本轮没有读取用户日常应用的可见性设置，不能据此确定此前该实例缺少 Grok 图标的具体原因。

已在简体中文和英文下分别检查第三方 CLI 设置页、本地任务窗口上方说明与权限选项、下方附件／技能／输入及任务控制按钮。47 张实际截图位图尺寸为 `1229 × 768`，这些控件可以通过垂直滚动访问，未观察到本轮目标控件的文字截断或按钮互相覆盖。截图全部实际编码为 JPEG，其中 34 张原文件后缀为 `.png`；保留原始字节，不转码或由文件后缀推断编码。语言切换后，正常退出空任务应用并重开，使完整英文界面生效；这不是任务重启恢复验收。

证据为 `01`、`02`、`03`、`04` 的简体中文截图，以及 `06`、`07`、`08`、`09` 的英文截图。`05` 记录语言变更提示，此时部分旧界面尚未完整切换，不作为完整英文布局证据。恢复的设置标签页仍保留先前的中文标签名；本报告不声称全应用的所有持久化界面名称已经重新本地化。

## Claude 真实两轮交互

使用用户已明确授权的 Claude API，凭据仅加载到隔离应用进程环境，不写入本报告或新增的凭据文件。验收模型为 `claude-sonnet-4-6`。隔离项目的 `Write`／`Edit` 要求审批，不添加绕过审批参数。

输入框实际接收了中文、英文和三行文本，截图 `10` 保留提交前草稿。Computer Use 的粘贴接口返回等待剪贴板超时，但随后真实截图显示文本已完整插入；没有重复粘贴，也没有把这个工具超时计作产品输入失败。目录最初经键盘模拟输入后未通过应用的目录校验，SQLite 中任务数仍为零；改用粘贴修正目录后才创建任务。截图 `11`—`14` 记录这次未创建任务的操作，不计原生启动成功；`15` 记录真正的新任务。

实际任务 ID 为 `2810123a-1c6c-4f90-b5d0-3529979521ed`，原生 Claude 会话 ID 为 `df83284c-f959-4176-a169-dfd4b97350b3`。第一轮结果匹配固定验收标记，第二轮在同一原生会话中正确返回第一轮标记和第二轮标记。两条输入在 SQLite 中均为 `acknowledged`，两个任务运行记录均有原生完成结果。

第一轮 [SQLite 证明](validation/gui-436cc739/17-claude-first-sqlite-proof.json) SHA-256 为 `95e94a6ff77fb5c72ee1bf9751544309fc78391e82c3a98fe70ba2ce7772ee36`；第二轮 [SQLite 证明](validation/gui-436cc739/20-claude-second-sqlite-proof.json) SHA-256 为 `f71e55c6eeef53e12b3758ee34c3649851e609f0bf60e8866254b38257425373`。两份证明以只读方式检查隔离应用数据库；公开结果正文仅为本次合成验收标记，不含用户历史任务。截图 `16`、`18`、`19`、`21` 与上述记录对应。

## 审批、追加和取消

同一任务第 3 轮实际等待 `Write` 审批，点击 `Allow once` 后才改写合成目标文件并回收 `GUI_ALLOW_DONE_436CC739`。第 4 轮点击 `Deny once` 后目标文件保持原字节，并回收 `GUI_DENY_CONFIRMED_436CC739`。这里的完成表示原生回合结束，不表示被拒绝的写入成功。截图 `22`—`34` 及对应的只读 SQLite／目标文件证明记录这两项操作；项目审批设置 SHA-256 仍为 `cd71a3552bff518dcae45edb70c5fc980c6ca92434179f74eadc71de82172a84`。

第 5 轮等待 `Write` 审批时，应用实际提交了追加消息 `69eac33b-1ab8-4abe-a675-0c07ea6aaeff`，原生接收确认与原提交代 5 一并持久化。随后点击 `Cancel turn`，第 5 轮保存为 `cancelled`，原输入 `44e52cfd-bdb7-4024-a7bd-cdb1cefd27bd` 的 `TurnFinished.outcome` 为 `Cancelled`，目标文件保持 28 字节与原 SHA-256。此前追加的指令作为第 6 轮实际执行，并以自己的输入 UUID 返回 `GUI_APPEND_436CC739`。应用已明确提示取消当前执行后，等待指令可能随后运行。本次不把接收确认描述成执行完成，也不把第 6 轮完成描述成第 5 轮未被取消。

[取消与追加的 SQLite 证明](validation/gui-436cc739/41-cancel-and-append-sql-proof.json) SHA-256 为 `72418aa79314b589ea0fee676dca6ec2b2eb27a521141e5cce1190d05ed4ad5f`，截图 `35`—`42` 与其对应。GUI 证明记录协调器持久化的原生终态；完整底层取消证据聚合另见 source26 的待 Edit 取消 2 独立原生验收，不由截图推断所有底层控制帧。

## 历史继续与应用重启

第 6 轮完成后实际断开 CLI，再点击 `Continue history`。这一步只启动历史会话连接，数据库仍只有此前 6 条输入；草稿不会自动发送。待连接就绪后明确点击 `Send instruction`，第 7 轮正确返回第一轮标记、追加标记和 `GUI_RESUME_436CC739`，原生会话 ID 保持不变。[历史继续证明](validation/gui-436cc739/48-claude-history-continue-sql-proof.json) SHA-256 为 `6ac8121d25c4a0e45892541b9ddcd563b8f9e272a5e20f4a703ae1090f85933c`。

随后通过应用退出快捷键正常关闭，启动新进程前确认旧应用 PID 及已记录的全部后代 PID 均已消失；未观察到退出码，不编造退出 0。以同一隔离应用、HOME、项目和数据域重开，原任务、原生会话 ID、7 轮记录、全部 7 条已确认消息和上一轮结果恢复，未新增输入。第 5 轮取消记录保持取消。[重启后不重放证明](validation/gui-436cc739/51-claude-restart-no-replay-sql-proof.json) SHA-256 为 `36bc8fd221cefd19b86345d7160325a7178dda3f8255812715b4a8726d6b18dc`。

重启后再次明确点击 `Continue history`，创建第 8 轮并连接原生历史，输入消息数仍为 7；[历史连接未重放证明](validation/gui-436cc739/55-claude-restarted-history-no-input-replay.json) SHA-256 为 `1a65441b983adc3d3a843a430e0a671303d41117d38a370d8788c00d8b81d600`。这是新进程继续历史会话，不是重新关联仍活跃的旧进程。第 8 轮的新指令明确发送后，实际返回第一轮标记、追加标记和 `GUI_RESTART_436CC739`，原生会话 ID 不变，原生完成事件和第 8 条消息接收确认均持久化；项目审批设置与被取消的目标文件仍保持原字节。[重启后的最终结果证明](validation/gui-436cc739/57-claude-after-restart-final-result-sql-proof.json) SHA-256 为 `86cd920878ad6a342f71c0d4ba0de07662767c1ac864d215f05454bb388a50ab`，截图 `58` 显示真实结果。

本轮 Claude 的 macOS 托管根任务基本链已走完：新建、两轮、一次审批允许／拒绝、追加指令、当前轮取消、历史继续、应用重启、记录恢复及最终结果回收。总计 8 条已确认输入、8 轮历史（7 Completed、1 Cancelled），保持同一原生会话。本结论限定实际436提交的隔离重签副本、Claude 2.1.273、API模型与 Inherit 项目审批；不扩大为三款 CLI、所有平台、父子权限或完整故障组合通过。

## 仍待完成

当前链不覆盖活跃任务重新关联、父子 GUI、真实中文输入法或完整异常组合。Codex、Grok 的同提交 GUI 完整链、父子任务与消息恢复、真实 SSH/tmux 完整链、相关平台和全工作区门禁也不能由本报告替代。Grok SDK9 与只读 policy1 的真实失败仍按各自独立报告保留。

本轮没有产品源码或用户文案变更；无需本地化变更。双语布局结论仅限上述已操作的组件和窗口尺寸。
