# Claude GUI 与应用更新恢复验证

本次使用真实 Claude Code 2.1.273 和用户授权的 API，在独立 HOME、项目、profile 与数据库中通过实际 GUI 操作。API 凭据仅由私有 Claude 启动器注入该子进程；应用、Codex、Grok 均不继承。没有修改用户全局 Claude 配置，也没有直接写入任务数据库。

前半程 GUI 来自干净提交 `e4738e1ea96ea727c15d51e2c31dbf34152814b6`，重启后使用仅补入 Grok 标题栏默认入口和平台测试限定的 `a245c639d307ababbc9b26e604e71318b8ee9522`。因此这是实际更新恢复路径，不能替代最终单一提交的完整生命周期验收。两份构建、独立包签名摘要及截图/只读 SQLite 快照保存在 [证据清单](validation/gui-e473-a245/manifest-evidence.json)。

应用任务 `f13f4d58-7cf8-4690-9399-41de3bf11b87`，原生会话 `4ce7cdad-d9a4-48fa-9e8c-eee8fc7761da`，全过程保持不变：

| 回合 | GUI 操作与真实结果 |
|---|---|
| 1、2 | 中英文及多行输入完整提交，两次原生接收确认，分别得到 `GUI_CLAUDE_ONE`、`GUI_CLAUDE_TWO`；首轮要求记忆随机标记 |
| 3 | 原生 Write 请求出现后点击单次允许，指定文件字节为 `GUI_CLAUDE_ALLOWED` 加换行，结果 `GUI_ALLOW_DONE` |
| 4 | 原生 Write 请求出现后点击单次拒绝，目标文件不存在，前一允许文件未变，结果 `GUI_DENIED_DONE` |
| 5 | 第一次取消尝试已晚于真实完成；保持 Completed，不计取消成功 |
| 6 | 新回合执行期间点击取消，原生适配器终态为 Cancelled；原生会话 ID 保持 |
| 7 | 同进程显式发送新指令，结果 `GUI_AFTER_CANCEL` |
| 应用退出/重启 | 正常退出并启动新包；原任务、历史结果、取消状态及 7 条消息保留，未自动重发，连接恢复入口需显式点击 |
| 8 | 点击“继续历史”，启动新进程恢复原 ID；提示中不携带答案，仍准确返回首轮 `GUI_CLAUDE_E473_7Q9Z`，结果持久化 |

输入自动化的粘贴接口返回超时，但实际文字已到达；每次提交前通过截图核对完整内容，没有因超时重复发送。直接模拟按键曾丢失中文/标点的草稿已在发送前替换，这不计为输入成功证据。

本次还验证 a245 新包的 Grok 标题栏入口：英文和简体中文 1229×768 窗口中三个图标及对应设置均无截断、重叠；手动隐藏后重启仍隐藏，重新开启立即显示。点击图标启动真实 Grok TUI 并进入登录界面，随后正常退出，未完成认证或发送模型输入。含临时登录验证码的截图不进入仓库。

仍未验收：运行中排队的新协调器代码、父子任务/父权限上限、Claude 图片与技能/评审完整链路、故障组合及最终同提交的各平台生命周期。本次 e473/a245 包未包含后续队列修改，不据此解除这些门禁。
