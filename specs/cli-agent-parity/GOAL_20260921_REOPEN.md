# 新 Goal 指令：补齐 CLI 对齐与自动升级

将以下内容复制到新任务。该指令创建新的持续 Goal，不依赖旧任务曾被标记 complete 的状态。

```text
请创建并持续执行一个新的 Goal，完成 InfiniShell-Desktop 中 Codex CLI、Claude Code、Grok Build 的支持对齐与自动升级，直到原计划全部验收满足、代码及配套产物提交推送。

先读：
/Users/zhishi/Tools/github/InfiniShell-Desktop/AGENTS.md
/Users/zhishi/Tools/github/InfiniShell-Desktop/specs/cli-agent-parity/HANDOFF_20260921_REOPEN.md

然后阅读同一工作树的 PLAN.md、CLI_AUTOUPDATE.md、能力矩阵、验证报告及相关技能。2026-09-21 复核已撤回此前“Goal 全部完成”的结论；以新交接文档列出的代码和收据为准，保留真实通过的成果。

在现有工作树继续：
/Users/zhishi/Tools/github/InfiniShell-Desktop
根目录已整理并快进到基线归档提交 e6e9d619318e87d0bb889df344c8570f97977e90，受测代码提交为 38a611773b8ee53860f9ab731b2476b9a40d1819。先核对实时状态，保留尚未提交的交接与整理文档，继续当前 codex/cli-agent-parity 分支或从当前 HEAD 创建 codex/ 续接分支。不要从 main 重新开始，不要 reset/clean 根目录，也不要直接 pop 整理前的完整 stash。原网页搜索修复已合入 main，历史 GUI 证据和旧工作状态已有完整归档，详情见 WORKSPACE_CLEANUP_20260921.md。validation-56 仅作干净历史参考，后续在根目录继续。

完整范围仍为 PLAN 的 P0–P5，加三款 CLI 的消费者自动升级、开发最新版对齐和手动渠道切换。新安装遵循已约定的官方默认正式渠道，已有安装跟随当前渠道；先核实官方最新版本及真实接口，保留三方协议、技能、输入与权限差异。

优先完成：
1. Grok 当时最新正式版的运行中追加、固定权限和允许/拒绝审批、技能、本地工具与子任务。目前 1.0.34 仍只有受限 P0 能力；不能只删除保护或修改版本白名单。
2. Grok 真实父子派发、双向原生 ACK、进度、自动结果回收及持久恢复；失败收据不能由租约注册、单次 inspect 或绿灯单测替代。
3. SSH/tmux 的真实 CLI 触发和产品接收验收；已有回环 hook 重放仅证明传输。核对远端版本、插件、路径、断连和 tmux 行为。
4. 审核消费者构建中的托管任务入口、三款自动升级与渠道持久化、真实 IME 和双语布局，补齐当前版本完整生命周期及平台证据。

常规技术决策、修复、测试和按门禁提交推送自主推进；大任务按不重叠写入域交给子代理并行处理。需要登录时先核对既有认证，只在确实缺失时请求我授权；不要在文档或聊天中保存密钥。遇到实质外部阻塞准确说明，并继续独立工作。全程简体中文，新增注释也使用简体中文。

维护“要求→实现→CLI版本→源码提交→模式/平台→收据→结果”的当前矩阵。明确区分功能缺失、实际失败、证据不足和可靠降级；保留历史失败，不把旧版本或旧提交的通过拼成当前完成。

完成门槛：
- 三款 CLI 分别通过：新建→两轮交互→审批允许/拒绝→追加→取消→继续→应用重启→恢复→结果回收；普通终端与托管任务分别留证。
- 父子权限、双向消息、原生接收确认、进度、结果及持久恢复完成。
- 插件缺失/不兼容、CLI 崩溃、重复/乱序/旧事件、消息重投、恢复失败、升级失败和事务恢复均有实际范围相符的验证。
- 英文/简体中文同步，实际布局与输入检查完成。
- cargo check -p warp、cargo test -p warp --lib i18n::tests、受影响模块及仓库适用完整门禁通过。
- 按跨平台验证技能，对包含实际修改的同一提交完成 macOS/Linux/Windows 相关验证；单独留存 SSH/tmux 与真实 CLI 证据。模拟、编译、skipped 和回放不能冒充实际交互通过。
- 交付代码、插件、夹具、发布所需文件、更新后的计划/能力矩阵/验证报告，并完成提交推送核对。

无需重新实现已通过部分，也不要因接手就反复跑全量构建。先定向闭环，再冻结提交做最终验证。缺少可靠事件时明确降级，只有真实完成才显示成功。恢复须区分关联活跃任务与新进程继续历史，防止重复执行。

只有原范围全部验收满足，才把新 Goal 标记 complete。存在未通过项或实质外部限制时如实保留；未经我明确调整范围，不能把必需功能关闭后计作已完成。
```
