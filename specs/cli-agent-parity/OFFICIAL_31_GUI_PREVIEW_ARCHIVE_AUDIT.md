# source31 历史布局 GUI 公共归档独立审计

本次仅审核已关闭的公共归档：`gui-preview-official-31` 原 14 个文件、[source31 构建记录](validation/macos-official-31-bundle.json)及[根代理 GUI 专报](OFFICIAL_31_GUI_PREVIEW_VERIFICATION.md)，共 16 个主输入。原 14 文件严格闭合为 8 JSON、4 JPEG、2 AX 文本；完整路径、字节数与 SHA-256 见[审计清单](validation/gui-preview-official-31/archive-audit.json)。本代理没有重演 GUI、OCR、Cargo、CLI 或网络，也未读取旧数据库、认证、私有日志、冻结树及二进制。

## 独立核对结果

四张 JPEG 均真实解码为 1229×768，完整像素解码通过。本代理实际查看归档图片，确认英文 `Image attachment`、中文“图片附件”可见且没有截断，首条消息的中英文多行文本正常换行，消息中没有展示序列化动作 JSON 或内部附件缓存路径。复制后的截图显示可读草稿及中文图片描述。截图不证明附件被重新提交，也不能独立证明之后清空草稿；Copy/Paste、清空及未发送操作由根代理 CU 记录说明，公开行哈希和零新输入与之相符。

两份 AX 文本的 Shell 输入说明和菜单语言分别对应英文、简体中文，但没有导出任务消息标签，因此附件标签核对以截图为依据。语言选择与正常重启属于根实际操作；本次没有重新操作设置。可见区域检查只覆盖此次附件历史专项，不扩大为全部控件或完整双语 GUI 门槛。真实技能历史布局明确未覆盖。

8 份公共 JSON 全部通过重复键、非有限数值拒绝及顶层字段闭合检查；嵌套历史摘要、进程快照、签名与环境字段亦逐项核对。环境字段只有独立 HOME、CLI 配置目录、固定 Shell/PATH/locale 及受审控制项，没有 API 或认证值。沿用旧 PNG 独立审计的六类定向凭据模式，在 JSON 原始序列化字节及解码字符串、AX 文本、JPEG 原始字节中均为 0。图片字节扫描不是 OCR，不能证明像素文字的所有潜在凭据形状。

## 来源、状态和持久化范围

构建记录 SHA-256 为 `51e4cd15c0dbf5a539c66f4890f4db02a71421fe5b2e6f963822fd204dd1c2b4`。公共 source31 inputs/gates/python 与构建记录引用一致：父提交 `0059ef1bf8438c5af7545c3101b880ec2d0b10e9`、111 输入、7 修改、未提交候选、`same_commit_verified=false`。主程序来源 SHA `5d0320978e1f8df4f88e8ee20fd78c3f925c31ec42aa0ac67d36f9f61db3bfa2` 与重签副本 SHA `501495704bfc4bfe45668cdec2092b36a2cccaa2022d14b1c9749e333359fb52` 在准备和视觉记录间一致。本代理只核公共元数据，未重新计算二进制或签名。本地 check/i18n11/Rust1430/Python443 的引用同样仅来自已归档报告，不计本次重跑。

任务 `f11f573b-504a-4423-bd04-94942633f19d` 对应[实际436的旧 PNG 专报](OFFICIAL_26_CLAUDE_PNG_GUI_VERIFICATION.md)及其[独立审计](validation/gui-claude-png-436cc739/archive-audit.json)。旧任务 3 代、3 条消息及 `[Text,LocalImage] → [Text] → [Text]` 与本轮公共元数据一致；截图显示旧原生会话。旧 `Completed` 是历史结果，不能计为 source31 新 CLI 完成或重新恢复原生连接。

初始完整行摘要为 tasks 3 行、generations 20 行、messages 20 行。`02/04/08/09` 四阶段的三表行数与完整行 SHA 均严格等于准备摘要，新原生输入均为 0。准备记录的原主库为 618496 字节，WAL 为 0 字节，SHM 为 32768 字节；字节数、SHA 格式、空 WAL 散列和主库散列关联全部一致。初次非空 SHM 拒绝、未启动应用、整库逐字节复制和 `immutable` 只读打开属于根准备记录及专报。本代理没有读取任何数据库或 sidecar，不能声称独立重验了整库复制字节或数据库打开步骤。

根最终清理记录与两次快照的应用 PID 46032/46379 及所属 PID 46037/46046/46382/46389 一致；两份退出后公开快照均为空，根记录声明全部已不存在。本代理未重新查询进程。退出码未观察，不能记为原生退出 0；认证未复制、API 环境未加载、零新原生输入均保留原始范围。

根专报的两处二进制 SHA 均找到一致公共元数据来源；本轮专报链接已逐项核对存在，超出授权的本地门禁文档仅检查路径、不读取正文。原 14 文件、构建 JSON、根专报及所读历史参考档案前后 SHA 保持不变。未发现公共归档矛盾；上述未独立重演和未覆盖项保留在审计 JSON，不计最终提交、其他平台、SSH/tmux、完整 CLI 链或 Goal 完成。本次仅文档归档，无需本地化变更。
