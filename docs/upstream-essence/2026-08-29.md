# 2026-08-29 上游精华巡检与吸收

## 基线

| 项目 | SHA / 数量 |
|---|---|
| 巡检起点 `main` | `d333551784b964b042d596bc34ba7236d8cdd2f5` |
| `origin/main` | `d333551784b964b042d596bc34ba7236d8cdd2f5` |
| `upstream/master` | `066ec71b736fc3755e29f58f733deadbdac3d1af` |
| 共同祖先 | `b076027deb2e8d1b98e1073f9cd0f20fbf75a8ae` |
| `main...upstream/master` 独有提交 | `954 / 238` |
| 上次已审 upstream | `e054075b8715ffe6d8c6c17aef7e12c4d9c30afa` |
| 本次新增上游提交 | 51 |
| 吸收分支 | `codex/upstream-essence-2026-08-29` |

本周期扫描了上次基线之后的 51 个上游提交，并用 `git cherry`、补丁检查与本地代码检索排除了已经等价实现或依赖已停挂产品链的改动。重点覆盖编辑器性能、Windows 终端与渲染稳定性、shell 集成、Agent/MCP 生命周期和测试基础设施。

## 建议优先吸收（已落地）

以下 5 项均已在独立分支按原上游逻辑拆分提交；cherry-pick 无冲突，并保留了 InfiniShell 禁用内置 Factory MCP 的本地产品决策。

| 上游候选 | 价值与涉及路径 | 冲突风险 / 吸收方式 | 本地提交 | 最小验证 |
|---|---|---|---|---|
| [`1c925e33`](https://github.com/warpdotdev/warp/commit/1c925e3330d7cbc9016d6a6cce59fe250bfd554d) #15128 | 限制 `EditDelta::layout_delta` 的 Rayon 扇出与单行布局工作量，避免大文件/超长行造成任务爆炸和内存峰值；`crates/editor/src/content/`、`crates/editor/src/render/` | 低；直接 cherry-pick | `bfba15d5d` | `cargo test -p warp_editor --lib` |
| [`9589305a`](https://github.com/warpdotdev/warp/commit/9589305a257367c206e8ed9466fbb6920794aba2) #15544 | PowerShell 5.1 不再注入仅 PowerShell 7 支持的 login-shell 参数，修复 Windows 默认环境启动失败；`app/src/terminal/model/session/command_executor/local_command_executor.rs` | 低；直接 cherry-pick | `81975bcf6` | Windows 上启动 Windows PowerShell 5.1，并确认新 tab 能进入 prompt |
| [`ad28e590`](https://github.com/warpdotdev/warp/commit/ad28e590f8781cac00c8b762425a19971a8549da) #15566 | WGPU 渲染前识别 device-lost 标志，让 Windows RDP 切换后走既有 renderer 恢复路径；`crates/warpui/src/rendering/wgpu/renderer.rs` | 低；直接 cherry-pick | `12af14dd9` | `cargo check --target x86_64-pc-windows-msvc -p warpui`，并在 Windows RDP 断开/重连后确认画面恢复 |
| [`607be8c2`](https://github.com/warpdotdev/warp/commit/607be8c26da633c882e53de2ab627730b6d28acc) #15518 | Bash bootstrap 将 `shell_plugins` 正确写入普通与 MSYS2 的 `Bootstrapped` payload，避免插件能力静默丢失；`app/assets/bundled/bootstrap/bash_body.sh` | 低；直接 cherry-pick | `ec24e6605` | `bash -n app/assets/bundled/bootstrap/bash_body.sh && bash -n app/assets/bundled/bootstrap/bash.sh` |
| [`9ee2fa1d`](https://github.com/warpdotdev/warp/commit/9ee2fa1d46f18779c1edff2d4eb34adeebf3f995) #15628 | 统一持久化与临时 MCP 的 reconnect 生命周期，让并发 waiter 复用同一次 spawn、完成后全部唤醒，并避免 shutdown 遗留竞态；`app/src/ai/mcp/templatable_manager*` | 中；直接 cherry-pick后复核本地产品约束 | `51a9bba09` | `cargo test -p warp --lib ai::mcp::templatable_manager::tests` |

## 继续观察

| 候选 | 决策与重新评估条件 |
|---|---|
| [`1e45ef77`](https://github.com/warpdotdev/warp/commit/1e45ef77359670ad911285b7cc79a61fa5bef8f5) #15574 | 共享自定义推理 endpoint 的方向与本地 BYOP/`agent_providers` 有交集，但上游补丁耦合 GUI/TUI 配置所有权。继续观察；若本地出现两端 provider 配置漂移，再按本地配置源手工移植设计。 |
| [`fc4d563b`](https://github.com/warpdotdev/warp/commit/fc4d563b8b6310e8533d98c5db762df484b87ad0) #15573 + [`d5d12d90`](https://github.com/warpdotdev/warp/commit/d5d12d90fe0584e34288156fcb804d27ddbde75c) #15425 | 原生 shell completion 统一走 in-band generator 并增加集成测试，长期有价值，但改动面覆盖所有 shell 与 bootstrap 协议。等待上游在真实 bash/zsh/fish/PowerShell 环境稳定一周期，再按本地 shell 集成差异手工移植。 |
| [`1a29f680`](https://github.com/warpdotdev/warp/commit/1a29f680d347c10e4a18a5b1550e5ffa3a9995f0) Agent recovery retry/resume budget | 上轮候选仍与本地 BYOP 恢复策略存在高冲突。本地主线本周期没有让它重新适用；仅在复现无限重试、预算失控或恢复状态覆盖时借鉴其状态机。 |

## 不建议吸收

| 候选 | 决策依据 |
|---|---|
| [`511b952c`](https://github.com/warpdotdev/warp/commit/511b952c257c3962b83f4b62f134c6e6a2747585) #15380 | `create_file` 的 `allow_overwrite` 扩大 Agent 文件覆盖能力；本地没有明确需求或风险证据支持放宽默认安全边界，因此本周期不吸收。 |
| [`21f413b7`](https://github.com/warpdotdev/warp/commit/21f413b7971709f5345fdaa03bdbda30875db979) #14875、[`2a183d55`](https://github.com/warpdotdev/warp/commit/2a183d5523309e52ff35d869eb7c7ae40e06bdea) #15462 | 大规模 crate 拆分与代码搬迁，主要是上游内部架构整理；对当前功能没有独立收益，且会制造高冲突，不吸收。 |
| [`f8aa4b98`](https://github.com/warpdotdev/warp/commit/f8aa4b98ebd43d931420e7f3161afd2131a60beb) #15622、[`061318ff`](https://github.com/warpdotdev/warp/commit/061318ff7fc424e41fbd77e30432995d483c99e4) #15583 | 分别依赖本地已停挂的 CLI Agent 会话同步和 Warp 私有 Oz/远端 child rendering 链；当前活跃路径不可达，套补丁只会形成死代码。 |

## 验证结果

### 已通过

- `git diff --check main...HEAD`。
- 仅对本次变更的 Rust 文件执行仓库 rustfmt 配置检查。
- `bash -n app/assets/bundled/bootstrap/bash_body.sh` 与 `bash -n app/assets/bundled/bootstrap/bash.sh`。
- `cargo test -p warp_editor --lib`：505 passed；其中本次新增/变动的编辑器布局测试 15 项通过。
- `cargo test -p warp --lib ai::mcp::templatable_manager::tests`：5 passed。
- `cargo test -p warp --lib i18n::tests`：7 passed。
- `cargo nextest run --no-fail-fast -p warp -E 'test(reconnect)'`。
- `cargo test -p warpui --lib rendering::wgpu::resources::tests`：命令通过；当前过滤条件没有匹配到单测，RDP device-lost 仍需实机验证。
- `cargo check -p warp` 与工作区默认成员 `cargo check`。
- `cargo check --target x86_64-pc-windows-msvc -p warpui`，覆盖 RDP renderer 的 Windows 条件编译路径。

### 已知验证边界

- 完整 `./script/format --check` 只报告 `app/src/workspace/view/left_panel.rs` 的两行 import 合并；该差异在巡检起点 `main` 已存在且与本次吸收无关，因此没有越权修改。所有本次变更 Rust 文件的定向 rustfmt 检查通过。
- 完整 Windows 目标和 Windows `warp_editor` 检查在第三方 `aws-lc-sys` 构建阶段停止：macOS 宿主缺少 Windows SDK 的 `stdlib.h` / `windows.h`，尚未进入项目代码；拆分后的 `warpui` Windows 检查已通过。
- Linux musl 检查在第三方 `aws-lc-sys` 构建阶段停止：宿主缺少 `x86_64-linux-musl-gcc`，尚未进入项目代码。
- 跨平台云验证要求远端可见的精确 commit。本分支尚未获授权推送，因此没有擅自 push 或触发 GitHub Actions。合并前仍需在 Windows runner 验证 PowerShell 5.1 启动与 RDP device-lost 恢复，并在 Linux runner 跑相关编译/测试。

## 本地化审计

无需本地化变更。本次改动没有新增或改变 GUI/TUI 用户可见文案；新增文本仅为内部错误/日志、shell bootstrap payload 和测试夹具。英文与简体中文资源一致性门禁已通过。

## 后续动作

1. 获得推送授权后，将本分支推到远端并按 `cross-platform-cloud-verification` 对同一精确 commit 运行 Windows/Linux 原生验证。
2. Windows 实机重点验证 PowerShell 5.1 新 tab、RDP 断开/重连后的渲染恢复；Linux 验证编辑器与 MCP 聚焦测试。
3. 合并后将本报告 5 项状态保持为“已吸收”；下周期从 `066ec71b736fc3755e29f58f733deadbdac3d1af` 之后继续扫描。
