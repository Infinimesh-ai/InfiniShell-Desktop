# 2026-09-22 上游精华巡检与吸收

## 基线与范围

| 项目 | SHA / 数量 |
|---|---|
| 实施起点 `main`、`origin/main` | `a6e61a1517c15c9250fb0443006d5c764842aab5` |
| `upstream/master` | `c02a1887c2ffac2fe1362140acc60f1c42dd614c` |
| 共同祖先 | `b076027deb2e8d1b98e1073f9cd0f20fbf75a8ae` |
| `main...upstream/master` 独有提交 | `1006 / 382` |
| 上一份目录报告已审 upstream | `3959ea72141fc0ecd007665029a8058e0e6db0f8` |
| 本任务上一轮即时巡检已审 upstream | `37a7ebeffaaaeb6f67cf7ad84d2f6bd2fb1a93e6` |
| 即时巡检后新增上游提交 | 1 |
| 吸收分支 | `codex/upstream-essence-2026-09-22` |
| 独立工作树 | `.worktrees/upstream-essence-2026-09-22/` |

本轮先执行 `git fetch upstream master`，再以补丁、`git cherry`、patch-id 和本地相关实现检索复核候选。5 组候选在本地主线没有完整等价实现，且都落在当前活跃编译路径；实施全部位于独立工作树，没有改动主工作树、合并、变基或推送。

## 建议优先吸收（已在独立分支落地）

| 上游 commit / PR | 价值与涉及路径 | 冲突风险 / 吸收方式 | 本地提交 / 最小验证 |
|---|---|---|---|
| [`c72f36d6a`](https://github.com/warpdotdev/warp/commit/c72f36d6a91bf0733c02b4a2c1f1709f5aa7645f) #14624 | 启动配置可保存并恢复标签组、固定组、活动 pane 和既有组连续性；`app/src/launch_configs/`、`app/src/workspace/`、`crates/integration/` | **中**；手工移植并适配本地 workspace API，带入序列化、恢复与集成测试 | `b058f5173`；`cargo test -p warp --lib launch_config` |
| [`0bb920b28`](https://github.com/warpdotdev/warp/commit/0bb920b2852f04f65aa6fe5a1f7627b862d25248) #15996 | 关闭再恢复父标签后，子 Agent pill 仍能导航到正确会话，且不会残留已关闭 surface；`app/src/ai/`、`app/src/pane_group/`、`crates/integration/` | **高**；手工移植生命周期清理、延迟导航和撤销恢复逻辑，不恢复本地不存在的上游 UI 分支 | `2dd61ee01`；`cargo nextest run -p warp -E 'test(closed_surface_cleanup) | test(test_ensure_hidden_child_agent_pane_restores_child_from_detached_group)'` |
| [`287888707`](https://github.com/warpdotdev/warp/commit/28788870759cebf8150065c277dbc650308ee7cd) #15963、[`4a7786c76`](https://github.com/warpdotdev/warp/commit/4a7786c767813e76883d6be6bbfc228e10911859) #15964、[`f62a045e0`](https://github.com/warpdotdev/warp/commit/f62a045e0fa9509524e5963b28989583d693c6b8) #16000、[`bb0a0593c`](https://github.com/warpdotdev/warp/commit/bb0a0593ce40edc7bcd0a2da615307128fb1b8f2) #16045 | 为 `computer_use` 增加 Windows `gdigrab` 录屏、跨平台路径与后处理、覆盖层和测试；`crates/computer_use/` | **中**；**部分吸收**上游最终 crate 状态。InfiniShell 已物理删除 Agent 录屏执行/上传链，因此不恢复 Warp 私有云 wiring 与 feature flag | `bafd643cd`；`cargo nextest run --no-fail-fast -p computer_use`，以及 `cargo check -p computer_use --target x86_64-pc-windows-msvc --tests` |
| [`2c6029018`](https://github.com/warpdotdev/warp/commit/2c6029018c492ab162ba2716d5675b82346b6dd9) #16055 | macOS 剪贴板识别 `public.tiff`；TUI 对不支持的 TIFF 保持文本回退，避免误走图片处理；`crates/warpui/`、`crates/warp_tui/` | **低**；手工移植并按 TUI 支持的 MIME 白名单补回归测试 | `f1e5c96c7`；`cargo nextest run -p warp_tui --features warpui/test-util -E 'test(classifies_unsupported_clipboard_image_data_as_text)'` |
| [`1378696ce`](https://github.com/warpdotdev/warp/commit/1378696ce8756ddf359442e97b9d671e3e7238c1) #16050 | Agent 空闲超时显式设为零时立即完成，并取消旧计时器，避免零时长仍异步排队；`app/src/ai/agent_sdk/driver.rs` | **低**；按本地 Agent 与 CLI harness 双路径手工移植共享 helper | `1996f96cc`；`cargo nextest run -p warp -E 'test(idle_timeout_sender_complete_with_zero_idle_sends_immediately)'` |

## 继续观察

| 上游 commit / PR | 结论 | 风险 / 重新评估条件 | 推荐方式 |
|---|---|---|---|
| [`c02a1887c`](https://github.com/warpdotdev/warp/commit/c02a1887c2ffac2fe1362140acc60f1c42dd614c) #15654 | 在 `SIGTERM` / `SIGINT` 时保存 handoff snapshot，并支持第二次信号立即退出；这是上一轮即时巡检之后唯一新增提交 | **高**；补丁同时重构 500 余行 Agent driver、依赖 Warp 云任务状态、handoff snapshot、录屏收尾和 Unix signal-hook。InfiniShell 已裁剪其中部分云录屏链，直接移植容易重新引入私有后端耦合 | 当前仅借鉴“双信号分级退出”和可测试的独立 termination 模块设计。待本地 handoff snapshot 成为稳定活跃路径，或出现信号退出丢失任务状态的明确故障后，再拆出最小本地实现 |

## 不建议吸收

本轮没有额外需要单列的高信号“不建议吸收”候选。品牌、计费、Warp 私有后端、内部发布流程和纯依赖升级仍按目录规则忽略或降权。

## 验证结果

### 已通过

- `cargo nextest run -p warp -E 'test(closed_surface_cleanup) | test(test_ensure_hidden_child_agent_pane_restores_child_from_detached_group)'`：3/3 通过。
- `cargo test -p warp --lib launch_config`：18/18 通过。
- `cargo nextest run --no-fail-fast -p warp -p warp_tui --features warpui/test-util -E 'test(idle_timeout_sender_complete_with_zero_idle_sends_immediately) | test(classifies_unsupported_clipboard_image_data_as_text)'`：2/2 通过；只有既有 TUI dead-code 警告。
- `cargo nextest run --no-fail-fast -p computer_use`：83/83 通过，2 个既有忽略测试跳过。
- `cargo check -p computer_use --target x86_64-pc-windows-msvc --tests`：通过，覆盖 Windows 录屏后端与测试编译。
- `cargo test -p integration --test integration --no-run`：通过，新增 GUI 集成测试可编译。
- `cargo test -p warp --lib i18n::tests`：11/11 通过。
- `cargo check -p warp`、`cargo fmt --all -- --check`、`git diff --check`：通过。
- `./script/run-tui`：在 macOS 本机成功构建并启动到 InfiniShell TUI 欢迎页，加载 `AGENTS.md` 与技能清单后正常退出。

### 验证边界

- 启动配置和子 Agent pill 的 GUI 集成测试本轮只完成编译，没有在桌面 GUI 中执行自动化交互。
- TIFF 实机验证覆盖了 macOS 剪贴板类型识别的编译路径、TUI 回退单测与真实 TUI 启动；没有向系统剪贴板注入实际 TIFF 文件做端到端粘贴。
- 分支尚未推送，因此没有可供 cross-platform preflight 精确绑定的远端 commit。本轮没有触发 Linux / Windows 自托管 runner；Windows 以 Rust 交叉目标完成 crate 与测试编译验证。

## 本地化审计

本轮改动为状态恢复、生命周期清理、录屏后端、剪贴板 MIME 分类和计时器语义，没有新增或修改标题、菜单、按钮、提示、错误、通知或无障碍文本，因此无需修改英文或简体中文 Fluent 资源。11 项 i18n 门禁全部通过；没有文案和控件布局变化，无需新增双语布局截图。

## 分支状态与后续续记

当前 5 项吸收均为本地提交，尚未合并到 `main`、推送远端或创建 PR。后续吸收任务需要在新的周期记录中续记：

1. 实际合并或 PR 的 commit / URL，以及 `main`、`origin/main` 最终 SHA。
2. 若分支推送后运行 cross-platform preflight，记录精确 `headSha`、workflow run 与 Linux / Windows job 结果。
3. 若执行 GUI 集成自动化或实际 TIFF 剪贴板端到端验证，补充对应命令、平台和结果。
4. 合并完成后将上述 5 项从“独立分支已落地”更新为“主线已吸收”；`c02a1887c` 保持“继续观察”，除非本地风险或产品路径发生变化。

下一周期从 `c02a1887c2ffac2fe1362140acc60f1c42dd614c` 之后继续巡检，并先核对本报告中的本地提交与后续状态，避免重复移植。
